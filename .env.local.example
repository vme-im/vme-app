# ========================================
# GitHub OAuth 应用配置（登录功能必需）
# ========================================
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# ========================================
# NextAuth 配置（登录功能必需）
# ========================================
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_nextauth_secret

# ========================================
# Cloudflare R2 配置（梗图上传功能）
# ========================================
R2_ACCOUNT_ID=your_cloudflare_account_id
R2_ACCESS_KEY_ID=your_r2_access_key_id
R2_SECRET_ACCESS_KEY=your_r2_secret_access_key
R2_BUCKET_NAME=vme-images
R2_PUBLIC_URL=https://img.example.com

# ========================================
# Neon 数据库配置（必需）
# ========================================
DATABASE_URL=postgresql://user:password@ep-xxx.aws.neon.tech/neondb?sslmode=require

# ========================================
# 数据同步配置
# ========================================
# GitHub Token (用于抓取 Issues，提高 rate limit: 60 → 5000/h)
# 创建方式: GitHub Settings > Developer settings > Fine-grained tokens
# 权限: Public Repositories (read-only)
GITHUB_TOKEN=github_pat_xxx

# 同步 API 密钥 (用于手动调用同步接口)
SYNC_API_KEY=your_random_secret_key

# Webhook 相关配置（自有仓库接入）
WEBHOOK_SECRET=your_webhook_secret

# 允许的 webhook 仓库 (JSON 格式，可选，默认跟随 SYNC_REPOS)
# WEBHOOK_REPOS='["vme-im/vme-content"]'

# 内容数据源（用于重复检测，可选）
CONTENT_DATA_URL=https://raw.githubusercontent.com/vme-im/vme-content/main/data.json

# LLM 标签分析配置（可选）
AI_API_KEY=sk-xxx
AI_API_BASE_URL=https://api.openai.com
LLM_MODEL=gpt-4o-mini

